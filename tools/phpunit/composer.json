{
    "require-dev": {
        "php-mock/php-mock-phpunit": "^2.1",
        "phpunit/phpunit": "^7.4"
    },
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
